# Database Connection (Server-side only)
DATABASE_URL='your-postgresql-connection-string'

# API URL for production builds
# For development, Vite proxy will be used (see vite.config.js)
# For production on Vercel, set this in Vercel environment variables
VITE_API_URL=https://your-ngrok-or-api-url.com
